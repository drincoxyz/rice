# launch program
super+{Return,d,w}
	${TERMINAL,LAUNCHER,BROWSER}

# launch terminal program
super+{y}
	$TERMINAL -e {straw-viewer}

# launch script
super+e
	cpemoji

# change volume
XF86Audio{Raise,Lower}Volume
	vol {up,down}
XF86{AudioMute,Launch1}
	vol toggle
